# One Node version per line ("version [default]"). Blank lines and lines
# starting with # are ignored. Add the literal word "default" as a second
# field to pin which version `nvm alias default` selects.
# Edit this file and run `chezmoi apply` to install newly added versions.

24 default
12
